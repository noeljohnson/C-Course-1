/*

The & operator returns its operand's location in memory

The * operator returns the value of the object to which its operand points

To accomplish pass-by-reference when passing a non-arry variable to a function, its necessary to pass the address of the variable to the function

*/
